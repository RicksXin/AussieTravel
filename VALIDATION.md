# 验证记录

2026-09-16

- `npm run build:weapp` 通过；微信产物位于 `dist/weapp/`，约 541 KiB。
- `npm run build:h5` 通过；Webpack 提示入口包超过其默认 244 KiB 性能建议，未阻断构建。
- `npm test`：4/4 通过，覆盖上海/墨尔本跨日时差、提醒筛选、ICS UTC/提前提醒/UTF-8 换行，以及源数据读取完整性。
- 在 390 × 844 的 H5 手机视口检查了单列布局、底部导航与底部弹出面板。
- 实际操作验证：分类筛选、SkyBus 搜索、完成标记及刷新后保留、补充时间及恢复原计划、清单勾选、原表备注展开、提醒页和路线页切换。
- 测试用勾选与时间修改已恢复，不替用户标记实际旅行进度。

未验证：真实微信 AppID 下的真机运行与 `.ics` 文件分享。微信订阅消息尚未接入；应用内提示仅在前台生效，离线提醒使用导入手机日历的方案。

## 2026-09-21 · 紧凑沉浸式头部

- 微信页面启用 custom 导航，依据状态栏和胶囊实际尺寸预留导航区域，海岸插画覆盖整个头部。
- 移除宣传卡片；日期改为双行紧凑选项，显式外边距保证间隔；完成数移入当天标题旁。
- 列表备注最多显示两行，详情仍保留全文；下一项安排移至列表底部。
- `npm test` 4/4 通过；微信与 H5 生产构建通过，H5 仍有入口包大小提示。
- H5 390 × 844 首屏可见两项完整行程及第三项标题；已验证切换日期、打开及关闭详情。
- 微信原生状态栏、胶囊避让的最终效果仍需微信开发者工具及真机确认。

## 2026-09-21 · 地图入口

- 从用户 Chrome 中「9月26日」My Maps 导出 KML，提取地点图层的 7 个 Point，排除路线吸附点；酒店 350 William St 与 Oaks 官网地址一致。
- 9/26 的 9 项安排有坐标；南十字星站和 Coles 未匹配，保留地名/Google 链接复制；Lune 提供独立入口。
- 微信调用 `openLocation`，取消不继续提示，失败可复制 Google 链接；H5 使用 Google Maps directions URL。
- `npm test` 7/7 通过，新增经纬度顺序、匹配范围、URL 编码、平台调用与失败/取消分支验证。
- 实际微信地图打开、澳洲地图显示及外部导航选择仍需真机确认。

## 2026-09-21 · 飞书系统更新同步（revision 946）

- 已重新读取4张工作表的全部文字范围：详细计划474行、汇总200行、景点库204行、路线图200行；各快照修订号一致且无截断。
- 更新9天84项安排、88项清单、22个景点备选，清除已解决的酒店/机场/旧路线冲突提示。
- 区分执行安排、参考时刻和待打磨草案；后两者不产生自动提醒或日历事件。返程落地事件使用上海 +08:00。
- 未变事件沿用旧ID，新事件不复用已删事件ID；未变清单标签保留ID。设备既有自定义时间保留；详情弹层仅展示行程信息与备注。
- 9项测试通过：新版关键航班与预约、失效路线排除、草案不提醒、双时区、快照完整性及地图行为。
- H5已交互验证9/30新版行程、旅途全览与22个景点备选弹层。
- 飞书的浮动图片（详细表57张、路线图7张）仍留在原表，本次没有上传图片或新增图片托管。
